fastapi>=0.110
uvicorn[standard]>=0.29
anthropic>=0.40
openai>=1.0
httpx>=0.27
python-dotenv>=1.0
pydantic>=2.6
pypdf>=5.0
# multipart 업로드(POST /api/admin/uploads/file). FastAPI 의 UploadFile/Form 이 요구합니다.
python-multipart>=0.0.9
# 회귀 테스트(backend/tests/ — 59개). 실행 전 `python -m app.db.seed` 가 필요합니다.
pytest>=8.0

# Vite dev 프록시가 /api 요청을 보낼 대상. 비워두면 http://localhost:8000.
# 앱 코드는 이 변수를 읽지 않습니다 — 어떤 값을 넣어도 요청은 항상 프록시를 탑니다
# (vite.config.js 의 server.proxy['/api'].target 으로만 쓰입니다).
VITE_API_BASE=http://localhost:8000
